Any *.php file in this folder will be setup to work with the autoloader. If a `vendor/autoloader.php` file exists, it will be included.

The autoloader will match files to functions or classes by name and namespaced pathing.